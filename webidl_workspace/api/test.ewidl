#if Version:atLeast("5.0") then
    /**
     * \brief Test module.
     * \version 5.0
     */
    [Exposed=Window]
    module Tizen {

        /**
         * \brief Hello world interface.
         * \version 5.0
         */
        [NoInterfaceObject, Constructor(int,int)]
        interface HelloWorld {
            attribute x;
            attribute y;
        };

        /**
         * \brief Goodbye world interface.
         * \version 5.0
         */
        [
            Constructor(int, int),
            Constructor()
        ]
        interface GoodbyeWorld : HelloWorld {
            attribute int x;
            attribute int y;
            attribute int z;
        };

    };
#end -- #if Version:atLeast("5.0") then
